import caffe
import numpy as np
from PIL import Image
import random

class ped_data_load_layer(caffe.layer):
	"""
	Directly load (input image, label) pairs for pedestrian classify task	
	Use this to feed data to beijing's deep but fast net
	"""
	
	def setup
	def reshape
	def forward
	def backward